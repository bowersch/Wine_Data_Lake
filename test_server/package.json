{
  "name": "wldp_dev",
  "version": "1.0.0",
  "description": "Setup:\r     npm init\r     ^downloads required node packages\r Before Useage:\r     update config.json with correct template and data files\r Run: \r     npm start\r     ^ command will run/update server continuously, so there is no need to rerun in between file updates",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon ./server.js"
  },
  "author": "Austin Chayka",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "express-handlebars": "^6.0.6",
    "nodemon": "^2.0.20"
  }
}
